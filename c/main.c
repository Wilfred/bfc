#include <stdio.h>

void eval_program(char* program, int program_length) {
    // Our cells are initialised to 0.
    char cells[30000] = {};
}

int main() {
    printf("hello world!\n");
    return 0;
}
